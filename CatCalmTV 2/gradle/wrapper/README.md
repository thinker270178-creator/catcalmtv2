# Gradle Wrapper JAR

This file (gradle-wrapper.jar) is a binary file that GitHub Actions will download automatically.

You don't need to include this file in your repository - GitHub Actions will generate it during the build process.

If you see this file mentioned in errors, it's normal - the build system handles it.
